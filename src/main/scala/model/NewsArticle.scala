package model

//TODO delete if not needed
case class NewsArticle(id: String,
                       longUrl: String,
                       crawlTime: String,
                       title: String,
                       intro: String,
                       text: String)
